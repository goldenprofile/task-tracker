[project]
name = "task-tracker"
version = "2.0.0"
description = "A desktop application for managing tasks and projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0.45",
    "nicegui>=2.0.0",
]
